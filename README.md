# Manipulação de Strings em JavaScript

Este projeto é um exemplo simples de manipulação de strings utilizando JavaScript puro, integrado a uma página HTML. Ele demonstra diferentes formas de concatenar textos e aplicar métodos comuns de strings, exibindo os resultados diretamente no navegador.

## 📚 Aprendizados

Com este projeto, foi possível aprender e praticar:
- Diferentes formas de concatenação de strings em JavaScript
- Uso de *template literals*
- Aplicação de métodos nativos de strings
- Manipulação do DOM para exibir conteúdos dinamicamente
- Estrutura básica de um arquivo HTML com JavaScript embutido

## ⚙️ Funcionalidades

- Concatenação de strings utilizando:
  - Operador `+`
  - Template literals (`` ` ` ``)
  - Método `concat()`
- Exibição do tamanho de uma string com `length`
- Conversão de texto para:
  - Letras maiúsculas (`toUpperCase()`)
  - Letras minúsculas (`toLowerCase()`)
- Captura de um caractere específico usando `charAt()`
- Renderização dinâmica dos resultados na página HTML

## 🛠️ Stack Utilizada

- **HTML5**
- **JavaScript (ES6+)**

## 🚀 Deploy

Este projeto pode ser executado localmente abrindo o arquivo `index.html` diretamente no navegador.  

## 📄 Licença

Este projeto está sob a licença **MIT**, permitindo uso, modificação e distribuição livre.

## 👤 Autor

**Gabriel Tarcia**
